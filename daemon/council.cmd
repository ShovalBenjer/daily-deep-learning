@echo off
rem הסדנה weekly curriculum council: Sundays. Diffs the living corpus and ingests.
cd /d C:\Users\shova\Downloads\daily-deep-learning
git pull -q
claude -p "You are the weekly CURRICULUM COUNCIL for this repo (cwd). Read course_plan.json (lifecycle block), corpus_manifest.json, ROUTINE.md section 0, and research_ladder.json. TASK: (1) Diff the living corpus against the manifest: list the local mirror 'C:\Users\shova\Downloads\new-recruit\Perplexity research' (authoritative when Drive is unreachable; try 'gws drive files list' on the folder id first, fall back to the local dir). (2) For each NEW or status=pending doc: read it whole, then ingest: add concrete topics to the right topic_pools, add at most 2 ladder items if it defines actionable study/build work, queue paper-card subjects for מעקב, add 1 judgment scenario idea if it contains a decision rule. Update its manifest entry to ingested with 'into' describing exactly what was added. (3) Review the past week's posts (posts/): list wrong-answered or low-confidence topics from the sync state (GET https://sadna-sync.shovalb9.workers.dev with Authorization: Bearer from daemon/.key) and append them to a 'review_backlog' array in corpus_manifest.json for future raids. (4) If today is within the last week of a season, draft 3 next-season spine proposals into corpus_manifest.json under 'season_proposals'. Commit ONLY the changed json files with message 'council: YYYY-MM-DD' and push. Never fabricate content; if a doc is unreadable, mark it pending with a note." --dangerously-skip-permissions --max-turns 50 >> daemon\fallback.log 2>&1
exit /b 0
