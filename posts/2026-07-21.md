# בוקר טוב 2026-07-21! מבוא: הבעיה שכל השאר נגזר ממנה

ברוכים הבאים ל"פוסט היומי שלנו", יום 1 מתוך 70. עשרה שבועות, הרצאה בשבוע, ארבעה quests ביום: מתמטיקה, קוד, Azure, ומחקר. מתחילים.

## Oracle Quest: MDP, התשואה, והטריק שממנו הכל מתחיל

מקורות השבוע: [Stanford CS224R, הרצאה 1](https://www.youtube.com/playlist?list=PLoROMvodv4rPwxE0ONYRa_itZFdaKCylL), ובהמשך (משבוע 3) [MIT 6.S184 Flow Matching](https://www.youtube.com/playlist?list=PL57nT7tSGAAUDnli1LhTOoCxlEPGS19vH).

### האינטואיציה

תחשבו על ההבדל בין ללמד ילד לרכוב על אופניים לבין להראות לו כרטיסיות של מילים. כרטיסיות זה supervised learning: הנה קלט, הנה התשובה הנכונה, תתקן את עצמך. אופניים זה משהו אחר לגמרי: אף אחד לא יכול להגיד לך "בשנייה 3.2 תטה את הגוף 4 מעלות שמאלה". אתה מנסה, נופל, מנסה שוב, ומקבל פידבק דל להחריד: נפלת או לא נפלת. מהפידבק הדל הזה המוח משחזר בדיוק אילו החלטות קטנות היו טובות.

זו כל התורה של **Reinforcement Learning, למידה מחיזוקים**: למידה מאינטראקציה, כשהעולם מחזיר רק reward, לא תשובה נכונה. שלושה דברים נשברים ביחס ל-supervised:

1. אין labels, יש רק אות סקלרי של "כמה טוב היה לך".
2. הדאטה לא i.i.d: הפעולה שבחרת עכשיו קובעת אילו מצבים תראה אחר כך. המודל משנה את ההתפלגות שהוא לומד ממנה.
3. **Credit assignment, ייחוס תרומה**: ה-reward מגיע באיחור. נפלת בשנייה 10, אבל הטעות קרתה בשנייה 4. מי אשם?

### למה זה יפה מתמטית

את כל הבלגן עוטפים באובייקט אחד, **Markov Decision Process, תהליך החלטה מרקובי**:

\[ \mathcal{M} = (\mathcal{S}, \mathcal{A}, P, r, \gamma, \rho_0) \]

מצבים, פעולות, דינמיקה \( P(s_{t+1} \mid s_t, a_t) \), פונקציית reward, מקדם הנחתה \( \gamma \), והתפלגות מצב התחלתי. היופי מסתתר בהנחת מרקוב:

\[ P(s_{t+1} \mid s_t, a_t, s_{t-1}, a_{t-1}, \dots) = P(s_{t+1} \mid s_t, a_t) \]

עכשיו תראו כמה יפה זה: ההנחה אומרת שהמצב הנוכחי הוא statistic מספיק של כל ההיסטוריה. כל העבר, אינסוף מסלולים אפשריים, נדחס לנקודה אחת במרחב המצבים. זו לא הנחה "מקלה", זו הגדרה של מה זה state טוב: ייצוג שהופך את העתיד לבלתי תלוי בעבר. חצי מהקושי בבעיות אמיתיות הוא לבנות ייצוג שמקיים את זה בקירוב.

### המשוואות המרכזיות

התשואה, ה-return, היא סכום ה-rewards המונחת:

\[ G_t = \sum_{k=0}^{\infty} \gamma^k \, r_{t+k}, \qquad 0 \le \gamma < 1 \]

סקיצת הוכחה למה \( \gamma < 1 \) הוא לא קישוט: אם ה-rewards חסומים, \( |r| \le r_{\max} \), אז

\[ |G_t| \le \sum_{k=0}^{\infty} \gamma^k r_{\max} = \frac{r_{\max}}{1-\gamma} \]

טור גיאומטרי מתכנס. בלי ההנחתה, באופק אינסופי, הסכום מתבדר וכל השוואה בין שתי policies מאבדת משמעות. **Discounting, הנחתה** קונה לנו well-definedness, ומקודדת העדפת זמן, בדיוק כמו ריבית.

המטרה: policy \( \pi_\theta(a \mid s) \) שממקסמת את התוחלת

\[ J(\theta) = \mathbb{E}_{\tau \sim p_\theta} \left[ \sum_{t=0}^{T} \gamma^t r(s_t, a_t) \right] \]

כאשר התפלגות המסלולים \( \tau = (s_0, a_0, s_1, \dots) \) מתפרקת יפה:

\[ p_\theta(\tau) = \rho_0(s_0) \prod_{t=0}^{T} \pi_\theta(a_t \mid s_t) \, P(s_{t+1} \mid s_t, a_t) \]

ועכשיו הרגע הכי יפה של היום. קחו לוגריתם וגזרו:

\[ \nabla_\theta \log p_\theta(\tau) = \sum_{t} \nabla_\theta \log \pi_\theta(a_t \mid s_t) \]

הדינמיקה \( P \) ו-\( \rho_0 \) לא תלויות ב-\( \theta \), אז הגרדיאנט שלהן נעלם. המשמעות מטלטלת: אפשר לשפר policy בלי לדעת איך העולם עובד. אין צורך במודל של הפיזיקה, רק ביכולת לדגום ממנה. זה הזרע של policy gradient, ומחר נצמיח ממנו את REINFORCE.

שימו לב גם ש-\( \pi_\theta(a \mid s) \) הוא מודל גנרטיבי: התפלגות מותנית שדוגמים ממנה. בשבועות הקרובים נראה שכשמרחב הפעולות עשיר, גאוסיאן פשוט לא מספיק, ושם ייכנסו diffusion policies ו-flow matching. שני הקורסים הם באמת קורס אחד.

### קוד אינטואיציה

```python
import torch

policy = PolicyNet(obs_dim, act_dim)   # outputs a distribution over actions

def rollout(env, policy, T=1000, gamma=0.99):
    s, _ = env.reset()
    traj, G, discount = [], 0.0, 1.0
    for t in range(T):
        dist = policy(torch.as_tensor(s, dtype=torch.float32))
        a = dist.sample()                       # we CREATE our own data
        s_next, r, done, trunc, _ = env.step(a.numpy())
        traj.append((s, a, r))
        G += discount * r                       # discounted return, on the fly
        discount *= gamma
        s = s_next
        if done or trunc:
            break
    return traj, G
```

כל היום נמצא פה: הלולאה יוצרת את הדאטה של עצמה, ה-return נצבר עם הנחתה, ואף שורה לא צריכה לדעת את \( P \).

## Crafting Quest: discounted returns מאפס

כתבו פונקציה `discounted_returns(rewards, gamma)` שמקבלת רשימת rewards באורך T ומחזירה רשימה שבה האיבר ה-t הוא \( G_t = \sum_{k \ge t} \gamma^{k-t} r_k \).

דרישות: מעבר אחד מהסוף להתחלה, O(T), בלי לולאה כפולה. בונוס: גרסה וקטורית ב-torch והשוואת זמנים. 15 עד 30 דקות, בלי להציץ. זה הלב החישובי של כל אלגוריתם שנראה החודש.

<details><summary>רמז, ואז פתרון ייחוס (לפתוח רק אחרי ניסיון)</summary>

רמז: אם יש לכם את \( G_{t+1} \), אז \( G_t = r_t + \gamma G_{t+1} \). רקורסיה לאחור.

```python
def discounted_returns(rewards, gamma):
    G = 0.0
    out = [0.0] * len(rewards)
    for t in range(len(rewards) - 1, -1, -1):
        G = rewards[t] + gamma * G
        out[t] = G
    return out
```

בדיקה מהירה: `discounted_returns([1, 1, 1], 0.5)` צריך להחזיר `[1.75, 1.5, 1.0]`.
</details>

## Azure Dungeon: AI-103, יום 1 של המסע

התחלת הקשת: **Plan and manage an AI solution** (25 עד 30% מהמבחן). היום, 40 דקות: בחירת שירות ומודל ב-Azure AI Foundry דרך [מדריך הלימוד הרשמי](https://learn.microsoft.com/en-us/credentials/certifications/resources/study-guides/ai-103), ואז שאלות התחזית למטה. נהלו gap list: מה שפספסתם חוזר בעוד יומיים.

שאלות predict-then-verify (ענו לפני שפותחים):

1. איזה שירות מאגד קטלוג מודלים, agents ו-projects תחת מטריה אחת?
2. פונקציית Azure צריכה לקרוא ל-endpoint של Foundry בלי מפתח בקוד. מה הדפוס הנכון?
3. במה שונה deployment מסוג Standard או GlobalStandard מ-ProvisionedManaged?

<details><summary>תשובות</summary>

1. Azure AI Foundry (לשעבר AI Studio): קטלוג, deployments, agents, evals תחת project אחד.
2. **Managed Identity, זהות מנוהלת**: keyless auth עם RBAC, למשל התפקיד Cognitive Services OpenAI User על המשאב. בלי מפתחות בקוד או ב-env.
3. Standard ו-GlobalStandard מחויבים לפי טוקן (idle עולה בערך אפס). ProvisionedManaged (PTU) מחויב לפי שעה בין אם השתמשת ובין אם לא, ולכן PTU יתום הוא כסף אמיתי שנשרף.
</details>

## Lore Quest: המקורות שנסרקים ממחר

מהבוקר הבא, המדור הזה חי: סריקה יומית של NVIDIA, DeepMind, Microsoft, Meta, Anthropic, HuggingFace, Mistral, EleutherAI ו-merlihson/scientific-resources, רק פריטים עם קישור אמיתי מהיממה האחרונה, ופעמיים או שלוש בשבוע כרטיס מאמר מלא עם וידוא סטטוס פרסום. היום, שני העוגנים של המסע:

- [CS224R Deep Reinforcement Learning, Stanford 2025](https://www.youtube.com/playlist?list=PLoROMvodv4rPwxE0ONYRa_itZFdaKCylL)
- [6.S184 Flow Matching and Diffusion Models, MIT](https://www.youtube.com/playlist?list=PL57nT7tSGAAUDnli1LhTOoCxlEPGS19vH)

לימוד פורה. מה דעתכם על זה?
